class LogisticRegression:
    def init epochs=10000, lr=
    def fit
    def train
z=xteta #(previsao)
h(z) #funcao sigmoide (f. no util.py)
grad=np.dot(X.T, (h-y))/y.size
    def probability
    def predict
    def cost

class LogisticRegressionReg(LogisticRegression):